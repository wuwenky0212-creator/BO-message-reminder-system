services:
  - type: web
    name: message-reminder-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn demo_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
